# Site Run 2026-08-12: Showcase Homepage Rebuild

Execution mode: portable-specialist (adapted). Reason: the user asked for an
orchestrator-planned, worker-implemented rebuild; concerns split cleanly into
data extraction and page implementation, and the Integrator keeps sole write
authority over `docs/DESIGN.md` and `docs/CONTENT.md`.

- Integrator (planning, contracts, verification): Cursor agent.
- Workers (implementation): Kimi K3 subagents.
- Contract: `docs/DESIGN.md` v0.8 (strict validation PASS, OKF usage PASS,
  10 active concepts / 10 bindings).
- Content source: `docs/CONTENT.md` (Real Inventory table is the only allowed
  source of on-page numbers).

## Work Orders

### WO-DATA (worker A, parallel)

- Cluster: Narrative/data.
- Task: Read all 35 OKF concept files under
  `skill/ultimate-design/references/design-okf/*/*.md` and all 19 playbooks
  under `skill/ultimate-design/references/*.md`; emit `/tmp/okf-data.json`
  with 54 entries: canonical path, domain, bilingual titles, faithful
  bilingual one-line summaries, keywords.
- Forbidden: writing to `docs/`, fabricating summaries.

### WO-PAGE (worker B, parallel)

- Cluster: Visual/interaction.
- Task: Implement `docs/index.html` per `docs/DESIGN.md` v0.8 and
  `docs/CONTENT.md`, with a placeholder explorer data block (3 sample
  entries) to be integrated later.
- Forbidden: editing `DESIGN.md`, `CONTENT.md`, or any file outside
  `docs/index.html`; inventing numbers; external JS; raster images.

### WO-INTEGRATE (worker B resumed)

- Task: Inline the WO-DATA JSON into the explorer data block; confirm count
  54 and filter behavior.

### WO-REPAIR (worker C, after Integrator verification)

- Task: Fix findings from validator runs and rendered review; scope limited
  to `docs/index.html`.

### WO-DEMO-1..6 (Grok 4.5 High workers, parallel; added at contract v0.9)

- Cluster: Visual/narrative, one work order per demo.
- User override: follow the reference site's real-case gallery approach;
  each worker runs the bundled ultimate-design skill end to end (SKILL.md
  operating loop, Build route, YOLO mode) to design one complete demo page.
- Deliverables per worker: `docs/demos/<slug>/index.html` (self-contained)
  plus `docs/demos/<slug>/DESIGN.md` (lean contract that must pass
  `validate_design_contract.py --strict-ultimate --require-frontmatter`
  and `validate_okf_usage.py`).
- Slugs: finlytics, teahouse, taskflow, petcare, deck-q2, gridnoise.
- Forbidden: editing files outside the demo folder; raster images; real
  brands; git commands.

### WO-BROWSE (computer-use agent, parallel)

- Task: Actually browse ui-ux-pro-max-skill.nextlevelbuilder.io (home,
  gallery, one live demo), capture screenshots, report the gallery card
  anatomy and demo-page mechanics for Integrator cross-check.

## Integration Ledger

- WO-DATA (Kimi K3): 54-entry JSON accepted after spot checks; inlined into
  the homepage by the rebuilt WO-PAGE run.
- WO-PAGE attempt 1 (Kimi K3) timed out without output; rebuilt and delivered
  by a Grok 4.5 High worker against contract v0.9.
- WO-DEMO-1..6 (Grok 4.5 High): all six demos delivered with per-demo
  contracts; repair rounds cleared 93 rendered-audit findings (mobile
  overflow, board clipping, in-slide zone markers, marker anchoring, target
  sizes, reveal fallbacks).
- Integrator final-mile edits on the homepage: marker re-anchoring to padded
  wraps, meta/footer target sizes, 700ms reveal fallback for full-page
  captures, nav label 样张 → 案例.
- WO-BROWSE cross-check: the reference gallery uses static image thumbnails
  and unattributed demos; this site keeps live iframe thumbnails and
  per-demo `DESIGN.md` links as deliberate differentiators.

## Verification Ledger

- `validate_design_contract.py --strict-ultimate --require-frontmatter`:
  PASS for the site contract and all six demo contracts.
- `validate_okf_usage.py`: PASS for all seven contracts (site 10/10 bound).
- Rendered UI audit (`validate_html_visual.mjs`, pinned Playwright 1.61.1 /
  Chromium rev 1228, desktop 1440x900 + mobile 390x844): PASS with 0 fails
  and 0 warnings for `docs/index.html` and all six demos.
- Supplemental QA harness (homepage): 26/26 — no horizontal overflow at
  320/375/768/1440 in zh and en, language switch with `aria-pressed`,
  explorer count/filter/empty state, workflow stepper, copy feedback,
  reduced-motion, and no-JS content visibility.
- Integrator screenshot review: homepage hero/gallery/explorer plus all six
  demos at desktop and mobile.
